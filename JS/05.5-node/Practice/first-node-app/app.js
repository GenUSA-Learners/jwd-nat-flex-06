// open the 'first-node-app' folder in your terminal
// in the 'first-node-app' folder in the terminal, run `node app.js` to see the output

// console.log(module);

const logger = require('./logger');

logger.log('message');
